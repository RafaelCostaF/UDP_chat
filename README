<h1>UDP Chat<h1/>

A simple chat project using UDP communication in Java. It comprises a server and a client. The server receives and displays messages from clients. The client sends messages to the server. Run the server and client in separate terminals. Type "bye" to end the connection. 
